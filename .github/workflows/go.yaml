name: Go

on:
  push:
    paths:
      - '.github/workflows/go.yaml'
      - 'go/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: 1.26.4
      - run: |
          cd go/mux
          go test
