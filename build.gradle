plugins {
    id 'java'
    id 'groovy'
    id 'scala'

    id 'org.springframework.boot' version '2.3.4.RELEASE' apply false
    id 'io.spring.dependency-management' version '1.0.10.RELEASE' apply false
    id 'io.ratpack.ratpack-groovy' version '1.8.0' apply false
    id 'com.github.maiflai.scalatest' version '0.27' apply false
}

apply from: 'gradle/versions.gradle'

subprojects {
    repositories {
        jcenter()
    }
}

test {
    useJUnitPlatform()
}
